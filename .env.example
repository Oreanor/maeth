VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Talking to the pieces. Either key is enough; with neither, the cloud on the
# board simply never appears (same providers as Star Elite).
VITE_GROQ_API_KEY=
VITE_OPENROUTER_API_KEY=
# Необязательно: свой список моделей через запятую, если провайдер вывел
# нынешние из обращения (тогда запросы отвечают 404).
VITE_GROQ_MODELS=
VITE_OPENROUTER_MODELS=
